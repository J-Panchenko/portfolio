import CustomButton from './CustomButton/CustomButton';
import IconButton from './IconButton/IconButton';

export { IconButton, CustomButton };

//TODO POINT
// Button folder is for all buttons content
// Don't create a new folder for each button
// Use composition and add new props to create new buttons
